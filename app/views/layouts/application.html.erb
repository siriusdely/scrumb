<!DOCTYPE html>
<html>
  <head>
    <title>Scrumb</title>
    <%= yield :header_scripts %>

    <%= csrf_meta_tags %>
  </head>
  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>

    <%= yield :footer_scripts %>
  </body>
</html>
